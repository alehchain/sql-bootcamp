# Laboratório — IN

1. Liste pedidos em três status usando `IN`.
2. Reescreva a mesma regra com `OR`.
3. Liste produtos das categorias 1, 2 e 4.
4. Liste clientes que possuem pedidos com subquery.
5. Teste conceitualmente `3 NOT IN (1, 2, NULL)`.
6. Proteja uma subquery com `IS NOT NULL`.
7. Documente a diferença entre `IN`, `NOT IN` e `EXISTS`.
